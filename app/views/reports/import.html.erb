# encoding: UTF-8

<%# source: http://eparreno.com/2009/05/14/how-to-import-data-from-csv-files-in-rails/ %>

<%
require 'csv'
CSV.open('public/upload/reports.csv', 'r').each do |row|
  Report.create(:date => DateTime.strptime(row[0], "%d/%m/%Y"), :duration => row[6], :comment => row[5], :project_id => row[2], :service_id => row[3])
  end

%>

<h1> Import </h1>

If the Browser has finished loading, the import should be complete.