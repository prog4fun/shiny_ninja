# encoding: UTF-8

<div class="row-fluid">
  <div class="span1 offset8">
    <%= link_to_new(:controller => "reports", :action => "new") %>
  </div>
</div>

<div class="row-fluid">
  <div class="span9">
    <%= paginate @reports %>

    <table class="table table-bordered table-hover table-condensed">
      <tr>
        <th><%= t("labels.report.date") %></th>
        <th><%= t("activerecord.models.customer") %></th>
        <th><%= t("labels.report.duration") %></th>
        <th><%= t("labels.service.wage") %></th>
        <th colspan="3"><%= t("labels.actions.head") %></th>
      </tr>

      <% @reports.each do |report| %>
        <tr>
          <td><%= report.date %></td>
          <td><%= get_customer_of_report(report) %></td>
          <td><%= report.duration %></td>
          <td><%= report.service.wage %></td>
          <td style="width:5%"><%= link_to_show(report) %></td>
          <td style="width:5%"><%= link_to_edit(:controller => "reports", :action => "edit", :id => report.id) %></td>
          <td style="width:5%"><%= link_to_destroy(:controller => "reports", :action => "show", :id => report.id) %></td>
        </tr>
      <% end %>
    </table>

  </div>

  <div class="span3">
    <%= render "search_bar" %>
  </div>
</div>