# encoding: ASCII

Herzlich Willkommen <%= @resource.firstname.encode("ASCII", :invalid => :replace, :undef => :replace) %> <%= @resource.lastname.encode("ASCII", :invalid => :replace, :undef => :replace) %>,

Sie koennen Ihren Account bestaetigen, indem Sie den folgenden Link anklicken:

<%= link_to 'Account bestaetigen', confirmation_url(@resource, :confirmation_token => @token) %>

Danach koennen Sie sich mit Ihrem Benutzernamen und Ihrem Passwort anmelden.

Falls Sie den Account zum Zeit auswerten erstellt haben, koennen Sie mithilfe des Links in der vorherigen E-Mail das Projekt hinzuf?gen.

Viel Spass mit der Applikation!
