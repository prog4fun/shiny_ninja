# encoding: ASCII

Hallo <%= @resource.firstname.encode("ASCII", :invalid => :replace, :undef => :replace) %> <%= @resource.lastname.encode("ASCII", :invalid => :replace, :undef => :replace) %>,

Es wurde ein Link angefordert, um das Password zurueckzusetzten.

Mit dem nachfolgenden Link, koennen Sie ein neues Passwort waehlen:
<%= link_to 'Passwort aendern', edit_password_url(@resource, :reset_password_token => @resource.reset_password_token) %>


Falls Sie keinen Link angefordert haben, koennen Sie diese E-Mail ignorieren.
Solange der Link nicht angeklickt wird oder Sie Ihr Passwort in den Benutzereinstellungen aendern, wird auch Ihr
Passwort nicht geaendert.

